fn_estim <- function(dbin, dbout, radix = "kfold_LM"){
  lm(dbin[]$)
  SPDE_LMOK_krigsim(dbin, dbout,
                    model = m_est,
                    mesh  = mesh,
                    nsim  = 0, radix = radix, verbose = FALSE)
}